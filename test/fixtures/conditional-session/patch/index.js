'use strict';

const setup = require('./setup');
const patchSetup = require('./patch-setup');
const requiredOverrides = require('./required-overrides');

module.exports = {
    setup,
    patchSetup,
    requiredOverrides,
};
